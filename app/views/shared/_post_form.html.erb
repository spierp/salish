<%= form_for(@post) do |f| %>
	<%= render 'shared/error_messages', object: @post %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Compose new post...", class: "compose" %>
	</div>
	<%= f.submit "Post", class: "btn btn-large btn-primary"%>	
<% end %>	